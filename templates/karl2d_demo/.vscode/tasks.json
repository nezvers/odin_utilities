{
    "version": "2.0.0",
    "command": "",
    "args": [],
    "tasks": [
        {
            "label": "Build Debug",
            "type": "shell",
            "command": "odin build demo -strict-style -vet -debug -out:${config:project_name}.exe",
            "group": "build"
        },
        {
            "label": "Build Release",
            "type": "shell",
            "command": "odin build demo -strict-style -vet -no-bounds-check -o:speed -out:${config:project_name}.exe",
            "group": "build"
        }
    ]
}