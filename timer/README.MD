# Timer

Timer utility for Single/Loop functionality. Uses callback slice that will be called when hitting zero.

`odin run demo`
Loop timer example with keyboard inputs:
- Backspace / Delete - remove digits
- Numbers - add digits
- Enter - set and reset timer
- Space - activate/ deactivate