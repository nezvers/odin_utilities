{
    "version": "2.0.0",
    "command": "",
    "args": [],
    "tasks": [
        {
            "label": "Build Debug",
            "type": "shell",
            "command": "odin build main -strict-style -vet -out:${config:project_name}.exe -debug",
            "group": "build",
            "linux": {
                "command": "odin build main -strict-style -vet -out:${config:project_name}.bin -debug"
            },
            "osx": {
                "command": "odin build main -strict-style -vet -out:${config:project_name}.bin -debug"
            }
        },
        {
            "label": "Build Release",
            "type": "shell",
            "command": "odin build main -strict-style -vet -no-bounds-check -o:speed -out:${config:project_name}.exe",
            "group": "build",
            "linux": {
                "command": "odin build main -strict-style -vet -no-bounds-check -o:speed -out:${config:project_name}.bin"
            },
            "osx": {
                "command": "odin build main -strict-style -vet -no-bounds-check -o:speed -out:${config:project_name}.bin"
            }
        }
    ]
}